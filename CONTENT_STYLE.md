# 内容写作规范

## 一、格式规范

### 禁止使用的格式

**禁止使用 ASCII 框线风格**，例如：

```
❌ 错误示例：

┌─────────────────────────────────────────────────────────┐
│                  Python 数据分析优势                      │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  ✅ 语法简单，接近自然语言，新手友好                        │
│                                                          │
│  ✅ 生态丰富，Pandas/NumPy/Matplotlib 等库成熟            │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

### 推荐使用的格式

**使用表格**：

| 优势 | 说明 |
|-----|------|
| 语法简单 | 接近自然语言，新手友好 |
| 生态丰富 | Pandas、NumPy、Matplotlib 等库成熟 |
| 处理大数据 | 百万行数据轻松搞定 |

**使用简洁的文字列表**：

- 语法简单，接近自然语言，新手友好
- 生态丰富，Pandas、NumPy、Matplotlib 等库成熟
- 处理大数据，百万行数据轻松搞定

**使用有序列表**：

1. 第一步：下载数据
2. 第二步：清洗数据
3. 第三步：分析数据

---

## 二、Markdown 语法规范

### 推荐使用

| 类型 | 语法 | 示例 |
|-----|------|------|
| 标题 | `# ## ###` | `# 一级标题` |
| 表格 | `| | |` | 见上方示例 |
| 无序列表 | `-` 或 `*` | `- 列表项` |
| 有序列表 | `1. 2. 3.` | `1. 第一步` |
| 代码块 | ` ``` ` | ` ```python ` |
| 强调 | `**粗体**` | `**重要**` |
| 引用 | `>` | `> 提示文字` |

### 避免使用

- 不要用 ASCII 字符画框线（`┌ ┐ └ ┘ ├ ┤ ┬ ┴ ┼ │ ─`）
- 不要用过多的横线分隔（`---` 只用于章节分隔）
- 不要用特殊符号做装饰（如 `✅ ❌ ⚠️` 大量使用）

---

## 三、内容结构规范

### 文章结构

```markdown
# 章节标题

> 简短介绍（1-2句话）

**学完这章你能干啥？**
- 能力1
- 能力2
- 能力3

---

## 一、大标题

### 1.1 小标题

正文内容...

**重点提示**：关键信息

### 1.2 小标题

正文内容...

**代码示例**：

```python
# 代码
```

**表格总结**：

| 项目 | 说明 |
|-----|------|
| xxx | xxx |

---

## 二、大标题

...

---

## 总结

| 内容 | 要点 |
|-----|------|
| xxx | xxx |
```

### 表格使用场景

- 对比多个选项（如工具对比、方法对比）
- 展示数据统计（如指标、数值）
- 说明步骤流程（如操作步骤）
- 总结要点

### 列表使用场景

- 罗列特点、优势
- 列出步骤
- 展示多个选项

---

## 四、示例对比

### 错误示例

```
┌─────────────────────────────────────────────┐
│              运营团队面临的困境               │
├─────────────────────────────────────────────┤
│  用户太多，无法逐一分析                       │
│  营销预算有限，不知道优先投给谁               │
│  高价值用户流失了才发现                       │
│  运营活动效果难以评估                         │
└─────────────────────────────────────────────┘
```

### 正确示例

**运营团队面临的困境**：

- 用户太多，无法逐一分析
- 营销预算有限，不知道优先投给谁
- 高价值用户流失了才发现
- 运营活动效果难以评估

---

## 五、检查清单

写作完成后，检查以下项目：

- [ ] 没有使用 ASCII 框线（`┌ ┐ └ ┘` 等）
- [ ] 表格用于对比、统计、步骤说明
- [ ] 列表用于罗列要点
- [ ] 代码块有语言标识
- [ ] 标题层级正确（# > ## > ###）
- [ ] 有总结表格或列表
